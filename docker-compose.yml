services:
  db:
    image: postgres:16
    container_name: wompi_db
    environment:
      POSTGRES_USER: wompi
      POSTGRES_PASSWORD: wompi
      POSTGRES_DB: wompi_test
    ports:
      - "5432:5432"
    volumes:
      - wompi_pg:/var/lib/postgresql/data

volumes:
  wompi_pg: